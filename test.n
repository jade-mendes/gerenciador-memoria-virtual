// Muitos olá

cont = mmap(0x1000, 4);

&vezes = "Quantos vezes processos criar?";

print_s(vezes);
input_n(cont);

label(loop1);
    cont = cont - 1;
    Create(test);
    print_s(nome);
    jump_gt(loop1, cont, 0);