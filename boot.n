// Muitos olá

&cont = mmap(0x1000, 4);

&vezes = "Quantos vezes você quer criar o processo test?";

print_s(vezes);
input_n(cont);

label(loop1);
    cont = cont - 1;
    Create(test);
    jump_gt(loop1, cont, 0);